#pragma once

#include <string>
#include <vector>

namespace uaii {
namespace cli {

int cmd_convert(const std::vector<std::string>& args);
int cmd_tokenize(const std::vector<std::string>& args);

}  // namespace cli
}  // namespace uaii
